[project]
name = "sovereign-edge-router"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "onnxruntime>=1.18,<2.0",
    "numpy>=1.26,<2.0",
    "transformers>=4.44,<5.0",
    "sovereign-edge-core",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
