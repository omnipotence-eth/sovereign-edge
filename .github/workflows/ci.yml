name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: astral-sh/setup-uv@v7
      - run: uv sync --all-packages
      - run: uv run ruff check .
      - run: uv run ruff format --check .

  type-check:
    name: Type Check
    needs: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: astral-sh/setup-uv@v7
      - run: uv sync --all-packages
      - run: uv run mypy --ignore-missing-imports packages/ agents/ services/
        continue-on-error: true

  security:
    name: Security
    needs: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: astral-sh/setup-uv@v7
      - run: uv sync --all-packages
      - run: uv pip install pip-audit bandit
      - name: Audit dependencies for CVEs
        run: uv run pip-audit
      - name: Static security analysis
        run: uv run bandit -r packages/ agents/ services/ -ll --quiet

  test:
    name: Test (Python ${{ matrix.python-version }})
    needs: [type-check, security]
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v6
      - uses: astral-sh/setup-uv@v7
        with:
          python-version: ${{ matrix.python-version }}
      - run: uv sync --all-packages
      - name: Run tests with coverage
        run: >
          uv run pytest -v --tb=short -x
          --cov=packages --cov=agents --cov=services
          --cov-report=term-missing
          --cov-fail-under=50
          --ignore=evals
          --ignore=agents/creative/tests
          --ignore=agents/spiritual/tests
          --ignore=packages/observability/tests
          --junitxml=test-results.xml
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-${{ matrix.python-version }}
          path: test-results.xml
